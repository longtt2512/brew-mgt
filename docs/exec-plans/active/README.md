# Active Exec Plans

One file per in-flight multi-step plan, created from
`docs/templates/exec-plan.md`. Keep this small — a few plans at most. When a
plan's exit criteria are met and verified, move it to `../completed/`.

_No active plans yet._

Likely first plan: `scaffold-spring-boot.md` (high-risk — requires a decision
record before any code is written).
