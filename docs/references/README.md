# References

Model-friendly external reference material, captured in-repo so agents do not
rely on chat history or live network access. Keep each reference focused and
note its source and capture date.

- [`brew-json-reference.md`](./brew-json-reference.md) — shape of the
  `brew --json=v2` output the adapter parses.

Add `*-llms.txt` condensed docs here for any external tool the project leans on
(e.g. Spring Boot testing, Testcontainers) when their full docs are too large to
keep in context.
